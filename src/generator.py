from src.models import Question, QuestionType

# TODO: Phase 2 — RAG-grounded question generation (single candidate)


def generate(question_type: QuestionType) -> Question:
    raise NotImplementedError

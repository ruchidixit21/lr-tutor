from src.models import Question, QuestionType

# TODO: Phase 4 — generate N candidates, score each, return best


def generate_gated(question_type: QuestionType) -> Question:
    raise NotImplementedError
